<!-- filepath: c:\Users\Ayush\OneDrive\Desktop\node js\Major-Project-main\views\home.ejs -->
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Home | Wanderlust</title>
    <link rel="stylesheet" href="/styles.css"><!-- Optional: link your CSS -->
</head>
<body>
    <h1>Welcome to Wanderlust!</h1>
    <p>Your adventure starts here.</p>
    <nav>
        <a href="/listings">View Listings</a>
        <% if (!currUser) { %>
            <a href="/login">Login</a>
            <a href="/register">Register</a>
        <% } else { %>
            <span>Hello, <%= currUser.username %>!</span>
            <a href="/logout">Logout</a>
        <% } %>
    </nav>
    <% if (success && success.length > 0) { %>
        <div class="flash-success"><%= success %></div>
    <% } %>
    <% if (error && error.length > 0) { %>
        <div class="flash-error"><%= error %></div>
    <% } %>
</body>
</html>